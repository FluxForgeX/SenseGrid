# Backend Configuration
# For localhost/Raspberry Pi: use 0.0.0.0 to allow external access
PORT=8000
HOST=0.0.0.0

# JWT Secret (CHANGE THIS IN PRODUCTION!)
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET=your-super-secret-jwt-key-change-in-production-min-32-chars

# Database (SQLite - works great on Raspberry Pi)
DATABASE_URL=sqlite:///./sensegrid.db

# CORS Origins (comma-separated)
# Add your Raspberry Pi IP if accessing from other devices on the network
# Example: http://192.168.1.100:5173 (frontend), http://192.168.1.100:5174
CORS_ORIGINS=http://localhost:5173,http://localhost:5174,http://127.0.0.1:5173,http://127.0.0.1:5174,http://localhost:3000

# Socket.IO
SOCKETIO_ENABLED=true
